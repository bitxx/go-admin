// ? 全局不动配置项 只做导出不做修改

export const HOME_URL: string = "/home";
export const LOGIN_URL: string = "/login";
export const PROFILE_URL: string = "/profile";
export const DICT_DATA_URL: string = "/admin/sys/sys-dictdata";
export const GEN_EDIT_TABLE: string = "/admin/sys/sys-tools/sys-gen/edit";

// * Tabs（黑名单地址，不需要添加到 tabs 的路由地址，暂时没用）
export const TABS_BLACK_LIST: string[] = ["/403", "/404", "/500", "/layout", "/login", "/dataScreen"];

// * 高德地图key
export const MAP_KEY: string = "";
